snippet .match
  match x {
  	_ => println!("{}", $1)
  }

snippet .filter
  .filter(|x| x )

snippet .each
  .for_each(|x| {})

snippet .fold
  .fold(0, |sum, x| sum + x)

snippet .find
  .find(|x| x )

snippet .dedup
  .dedup();

snippet .dedupb
  .dedup_by(|a, b| a.eq_ignore_ascii_case(b));

snippet .map
  .map(|x| x )

snippet PI
  std::f64::consts::PI
